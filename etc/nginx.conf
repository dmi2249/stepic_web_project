server {
    listen      80;
    server_name localhost;
    root        /home/box/web;

    location / {
        index   index.html index.php;
    }

    location ^~ /img/ {
        root /home/box/web/public/;
    }

}
